import { BRANDING_CONFIG } from '~/config/branding.js'

const { spacing } = BRANDING_CONFIG

export const SETTING = {
  paddingTop: spacing.paddingTop,
  paddingLeft: spacing.paddingLeft,
  textIndent: spacing.textIndent
}
